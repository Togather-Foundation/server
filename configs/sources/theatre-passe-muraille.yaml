name: "theatre-passe-muraille"
# Disabled: Elementor-built pages; no structured event containers found 2026-03-05.
# /25-26-season/ contains individual productions as h5 + plain <p> text (e.g. "Playing Feb. 1-21, 2026").
# Dates are embedded in Elementor text-editor paragraphs with no distinguishing CSS selectors.
# Individual show pages (e.g. /butch-femme/, /through-the-eyes-of-god/) are separate Elementor pages.
# Nav submenu under 25-26-season lists all show URLs, but each page has no consistent structured schema.
# /tickets/ is a Woocommerce/Elementor accordion (FAQ), not an event list.
# Re-enable only if show pages gain structured date markup (e.g. schema.org Event JSON-LD, <time> elements),
# or if a dedicated event listing page with CSS-selectable date fields is added.
url: "https://passemuraille.ca/25-26-season/"
tier: 0
schedule: "weekly"
trust_level: 7
license: "CC0-1.0"
enabled: false
max_pages: 3
