name: "theatre-passe-muraille"
# Disabled: Requires depth-2 (detail-page) scraping. 
# Season page (/25-26-season/) lists shows as h5 title + unstructured <p> dates (e.g. "Playing Feb. 1-21, 2026").
# Dates in season page are scattered across h5 + p tags with no CSS class selectors; no repeating container div.
# Individual show pages (e.g. /butch-femme/, /through-the-eyes-of-god/) have CLEAN structured dates
#   with "Show Dates" heading followed by "Sep. 20th – 27th, 2025" — much better quality.
# Nav submenu under 25-26-season lists all show URLs (butch-femme, what-brings-you-in, through-the-eyes-of-god, etc.).
# Workarounds: (1) Implement depth-2 scraping to extract from detail pages (requires scraper enhancement).
#              (2) NLP date parsing of "Playing MMM. D-D, YYYY" format (fragile, prone to extraction errors).
# Alternative: Contact venue to add <time> elements or schema.org Event JSON-LD to season page.
# Last review: 2026-03-09 — season has 6-8 productions; current structure unchanged since 2026-03-05.
url: "https://passemuraille.ca/25-26-season/"
tier: 0
schedule: "weekly"
trust_level: 7
license: "CC0-1.0"
enabled: false
max_pages: 3
