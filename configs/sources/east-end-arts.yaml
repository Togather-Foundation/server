name: "east-end-arts"
# Tier 1 (Colly CSS selectors). Homepage news/events grid confirmed 8 items 2026-03-05.
# Headless confirms article.vcex-recent-news-entry with h2.entry-title a (name + url).
# NO structured date element in the DOM; dates appear only in excerpt body text.
# /events/ returns 404; homepage carries recent programme announcements, not an events calendar.
# Disabled: start_date cannot be extracted without NLP on excerpt text — tier-2 detail scraping needed.
url: "https://eastendarts.ca/"
tier: 1
schedule: "weekly"
trust_level: 6
license: "CC0-1.0"
enabled: false
max_pages: 1
selectors:
  event_list: "article.vcex-recent-news-entry"
  name: "h2.entry-title a"
  url: "h2.entry-title a"
