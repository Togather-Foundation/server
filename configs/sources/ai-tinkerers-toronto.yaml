name: ai-tinkerers-toronto
# Tier 0: JSON-LD structured data on the page
# Platform: Custom Rails app + Tailwind CSS + JSON-LD (schema.org ItemList with Event objects)
# Organization match: AI Tinkerers Toronto (API unavailable for lookup; no ULID in database)
url: https://toronto.aitinkerers.org/
tier: 0
schedule: daily
trust_level: 5
license: CC0-1.0
domain: education
enabled: true
