name: "burdock-brewery"
# Disabled: Uses eventscalendar.co embed (data-project-id="proj_T8vacNv8cWWeEQAQwLKHb"),
# same widget vendor as luminato. The Shopify page renders the embed container but widget
# JS never populates it within Rod T2 timeout. No public eventscalendar.co API found.
# Earlier investigation found a Showpass API (showpass.com/api/public/events/?venue=17330)
# which may be a legacy/parallel ticketing integration — verify before using.
# Fix: contact venue or eventscalendar.co for API key/iCal export, or identify the
# underlying XHR endpoint via browser DevTools network capture.
# Related bead: srv-71948
url: "https://burdockbrewery.com/pages/music-hall"
tier: 2
schedule: "weekly"
trust_level: 6
license: "CC0-1.0"
enabled: false
