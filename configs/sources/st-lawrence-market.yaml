name: "st-lawrence-market"
# Disabled: Page returns a near-empty skeleton (~1261 bytes, only 2 CSS classes:
# "js" and "stylish-select") — the site fails to render meaningful content.
# Events likely JS-rendered; Tier 2 headless would be required, but given the
# minimal page skeleton observed, the site may have anti-bot measures or
# require session/cookie state to load events. Not viable without further investigation.
url: "https://www.stlawrencemarket.com/events/"
tier: 0
schedule: "manual"
trust_level: 7
license: "CC0-1.0"
enabled: false
