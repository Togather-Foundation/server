name: "obsidian-theatre"
# Tier 2 (DISABLED). WordPress Gutenberg season page with JS-rendered show cards (div.proditem).
# Successfully extracts: show title, image, URL to detail page.
# STRUCTURAL BLOCKER: No performance dates visible on listing page — all dates are on individual show detail pages.
# Inspection 2026-03-09: .meta div confirmed empty across all 2 listing cards. Detail pages link to Tarragon Theatre's
# ticketing system (EventId parameters) but embed no event dates in their HTML. Would require either:
#   (1) Post-processing: scrape detail pages individually to extract dates from ticketing links
#   (2) Third-party API: attempt to reverse-engineer Tarragon Theatre's event API
#   (3) Multi-step extraction: parse EventId from detail pages and correlate with Tarragon's public calendar
# None of these approaches are practical within current scraper architecture (T2/T3 scope).
# Organization match: no API lookup (server unavailable Mar 9 2026)
# Trust level: 8 (major Black-led theatre company in Toronto)
url: "https://www.obsidiantheatre.com/season-listings/"
tier: 2
schedule: "weekly"
trust_level: 8
license: "CC0-1.0"
enabled: false
max_pages: 3
headless:
  wait_selector: "div.proditem"
  wait_timeout_ms: 15000
selectors:
  event_list: "div.proditem"
  name: "p.title a"
  url: "a[href*='/show/']"
  image: "img"
