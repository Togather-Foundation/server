# Togather SEL — Source Configuration Template
# =============================================
# Copy this file (without the _ prefix) to create a new source.
# File naming: use lowercase, hyphens, no spaces. E.g.: toronto-symphony.yaml
#
# TIER 0 (JSON-LD extraction — preferred, zero config needed):
#   Works automatically on any site that embeds schema.org/Event JSON-LD.
#   No selectors needed. Test first: server scrape url <URL> --dry-run
#
# TIER 1 (Colly CSS selector scraping):
#   Required for sites without JSON-LD.
#   Must specify selectors for each field to extract.
#
# --- TIER 0 EXAMPLE ---
#
# name: "my-venue"
# url: "https://myvenue.ca/events"
# tier: 0
# schedule: "daily"
# trust_level: 7
# license: "CC0-1.0"
# enabled: true
# max_pages: 5
#
# --- TIER 1 EXAMPLE ---
#
# name: "my-venue"
# url: "https://myvenue.ca/events"
# tier: 1
# schedule: "weekly"
# trust_level: 6
# license: "CC0-1.0"
# enabled: true
# max_pages: 3
# selectors:
#   event_list: "div.event-card"
#   name: "h2.event-title"
#   start_date: "time[datetime]"
#   end_date: "time.end-date[datetime]"
#   location: "span.venue-name"
#   description: "p.event-description"
#   url: "a.event-link"
#   image: "img.event-image"
#   pagination: "a.next-page"

# --- REQUIRED FIELDS ---

# name: Unique identifier for this source (used in CLI commands).
# Must be lowercase with hyphens, no spaces.
name: "example-venue"

# url: The events listing page to scrape.
url: "https://example.com/events"

# tier: Extraction method to use.
#   0 = JSON-LD (automatic, no selectors needed)
#   1 = CSS selectors (requires selectors block below)
tier: 0

# --- OPTIONAL FIELDS ---

# schedule: How often to scrape (for future scheduling support).
# Values: "daily", "weekly", "manual" (default: "manual")
schedule: "daily"

# trust_level: Source data quality/trustworthiness (1–10, default: 5).
# Higher values give this source priority in conflict resolution.
# 8–10: Official institutional sources (symphony, museum, library)
# 5–7: Community venues, established arts orgs
# 1–4: User-generated, aggregator, or uncertain provenance
trust_level: 7

# license: Default license for events from this source.
# Most scraper sources should be CC0 or CC-BY — verify before setting.
# If unknown, omit this field (defaults to empty / unset).
license: "CC0-1.0"

# enabled: Set to false to temporarily disable without deleting the file.
enabled: true

# event_url_pattern: Only follow links matching this glob pattern (Tier 1).
# Useful to avoid scraping non-event pages during pagination.
# Example: "/events/*"
# event_url_pattern: "/events/*"

# max_pages: Safety limit for pagination to prevent runaway crawls (default: 10).
# Set lower for sites with deep pagination you don't need.
max_pages: 5

# skip_multi_session_check: Disable multi-session detection for this source (default: false).
# Set to true for sources that legitimately publish long-duration events (exhibitions,
# residencies, summer institutes, etc.) that would otherwise trigger the review queue.
# skip_multi_session_check: false

# multi_session_duration_threshold: Override the default 168h (1 week) duration threshold
# used by multi-session detection for this source. Use for sources that legitimately publish
# events spanning more than 1 week but less than your threshold (e.g. multi-week festivals).
# Value must be a valid Go duration string (e.g. "720h" for 30 days, "336h" for 2 weeks).
# Omit or leave empty to use the default (168h). Has no effect when skip_multi_session_check is true.
# multi_session_duration_threshold: "720h"

# --- TIER 1 SELECTORS ---
# Only required when tier: 1
# Use CSS selectors. For start_date/end_date, prefer elements with datetime
# attributes (e.g. <time datetime="2026-03-15">); the scraper will use the
# datetime attr value when present, falling back to element text.
#
# selectors:
#   event_list: "div.event-card"         # Container wrapping each event
#   name: "h2.event-title"              # Event name/title
#   start_date: "time[datetime]"         # Start date (prefers datetime attr)
#   end_date: "time.end-date[datetime]"  # End date (optional)
#   location: "span.venue-name"          # Location/venue name
#   description: "p.event-description"  # Description text
#   url: "a.event-link"                  # Link to event detail page
#   image: "img.event-image"             # Event image
#   pagination: "a.next-page"            # Pagination "next" link (optional)

# --- TIER 3 REST API EXAMPLE ---
#
# name: "my-api-venue"
# url: "https://myvenue.ca"
# tier: 3
# schedule: "daily"
# rest:
#   endpoint: "https://api.myvenue.ca/events"
#   results_field: "results"
#   next_field: "next"
#   url_template: "https://myvenue.ca/events/{{.slug}}"
#   field_map:
#     name: "title"                   # Flat key: top-level "title" field
#     image: "logo.url"               # Dot-notation: {"logo": {"url": "..."}}
#     start_date: "dates.start"       # Dot-notation: nested start date
#     end_date: "dates.end"
#     location: "venue.name"          # Dot-notation: nested venue name

# --- SITEMAP-BASED SCRAPING EXAMPLE ---
# Use when the listing page is unscrapeable (e.g. Wix Thunderbolt, Duda builder)
# but individual event detail pages contain structured data (JSON-LD, CSS, etc.).
# The scraper fetches the sitemap XML, filters URLs by pattern, and scrapes each
# matching URL using the source's configured tier. Works with tier 0, 1, or 2.
#
# name: "my-detail-page-venue"
# url: "https://myvenue.ca/events"           # metadata only (not scraped)
# tier: 0                                     # tier used for each detail page
# schedule: "daily"
# sitemap:
#   url: "https://myvenue.ca/sitemap.xml"     # sitemap XML to fetch
#   filter_pattern: "/events/.+"              # Go regex: only scrape event URLs
#   max_urls: 100                             # cap per run (default: 200)
#   rate_limit_ms: 500                        # delay between pages (default: 500)
#
# With tier 1 (CSS selectors on detail pages):
#
# name: "my-css-detail-venue"
# url: "https://myvenue.ca"
# tier: 1
# sitemap:
#   url: "https://myvenue.ca/sitemap.xml"
#   filter_pattern: "/shows/[^/]+$"
# selectors:
#   event_list: "article.event-detail"        # on the detail page
#   name: "h1.event-title"
#   start_date: "time[datetime]"
#   description: ".event-description"

# --- TIER 2 HEADLESS WITH INTERCEPT EXAMPLE ---
# Use when the site renders events via JS and the DOM never populates
# with structured data, but the underlying API response contains it.
#
# name: "js-api-venue"
# url: "https://myvenue.ca/events"
# tier: 2
# schedule: "daily"
# headless:
#   wait_selector: "body"
#   wait_timeout_ms: 15000
#   wait_network_idle: true
#   intercept:
#     url_pattern: "api\\.myvenue\\.ca/events"  # Go regex matching API URLs
#     results_path: "data.events"                # dot-notation to events array
#     field_map:
#       name: "title"
#       start_date: "dates.start"
#       end_date: "dates.end"
#       location: "venue.name"
#       image: "image_url"
# selectors:                                     # DOM fallback selectors (optional)
#   event_list: ".event-card"
#   name: "h2"
