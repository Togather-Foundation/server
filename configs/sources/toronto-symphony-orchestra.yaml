name: "toronto-symphony-orchestra"
# Corrected URL: /concerts-events 404s; the calendar lives at /concerts-and-events/calendar
# Disabled: site is a JS-rendered React SPA — static HTML has no Event JSON-LD.
# Fix: needs Tier 2 (Rod headless browser) — see bead srv-h264z.
url: "https://www.tso.ca/concerts-and-events/calendar"
tier: 0
schedule: "daily"
trust_level: 8
license: "CC0-1.0"
enabled: false
max_pages: 5
