# .gitignore for a Go project
# Ignore common build outputs, dependency directories, local files, and editor settings

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary and coverage outputs
*.test
coverage.out
cover.out
*.cover
*.out

# Build and output directories
/bin/
_build/
dist/

# Vendor directory (when vendoring dependencies)
/vendor/

# Go module download cache (relative path)
pkg/mod/

# Go workspace file (optional to ignore if local)
go.work

# Local environment and config files
.env
.env.*.local
.envrc
*.local

# IDE and editor settings
.vscode/
.idea/
*.iml
*.sublime-workspace
*.sublime-project

# OS-specific files
.DS_Store
Thumbs.db

# Logs and temporary files
*.log
*.tmp
tmp/
temp/

# Database / generated files
*.sqlite3
*.db

# Node (if frontend parts exist)
node_modules/

# Coverage and reports folders
coverage/
reports/

# Ignore swap files and backup files
*~
*.swp
*.swo

# Optional: add project-specific ignores below
# e.g. Docker, CI, or editor-specific files

# skills
.agent/
.agents/
.claude/
.claude
.serena/
.opencode/
.openpackage/

# github agents (spec kit related)
.github/agents/
.github/prompts/

# Spec kit
.specify/

# beads
# beads state (kept on beads-sync)
.beads/issues.jsonl

# Deployment files
# Environment files (allow .env.example for documentation)
*.env
!*.env.example
# Explicit exclusions for deployment environment files
deploy/config/environments/.env.development
deploy/config/environments/.env.staging
deploy/config/environments/.env.production
# Deployment configuration files (gitignored, but allow examples)
.deploy.conf.*
!.deploy.conf.example
# Caddyfiles (allow examples for documentation)
deploy/Caddyfile
!deploy/Caddyfile.example
deploy/docker/Caddyfile
!deploy/docker/Caddyfile.example
# Deployment state and lock files
deployment-state.json
*.state
*.lock
# Deployment logs in deploy/ directory
deploy/*.log
deploy/docker/.env
.env.backup
.env.original
/server


# Testing files
events.json

# Auto-generated web files (generated during Docker build)
web/robots.txt
web/sitemap.xml