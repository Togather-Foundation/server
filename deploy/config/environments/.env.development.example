# Development Environment Configuration Template
# Copy this file to .env.development and update with actual values
# IMPORTANT: Never commit .env.development to version control

# =============================================================================
# Database Configuration
# =============================================================================
# PostgreSQL connection string for development database
# Format: postgresql://user:password@host:port/database
# Required extensions: postgis, pgvector, pg_trgm
# Change CHANGE_ME to your actual password
DATABASE_URL=postgresql://togather:CHANGE_ME@localhost:5432/togather_dev

# =============================================================================
# Authentication & Security
# =============================================================================
# JWT secret key for signing authentication tokens
# SECURITY: Generate a strong random value for production
# You can generate one with: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_development_secret

# Admin API key for administrative operations
# SECURITY: Generate a strong random value for production
# You can generate one with: openssl rand -hex 32
ADMIN_API_KEY=CHANGE_ME_dev_api_key

# =============================================================================
# Application Configuration
# =============================================================================
# Logging verbosity level
# Options: debug, info, warn, error
# Use "debug" for development, "info" or "warn" for production
LOG_LEVEL=debug

# Deployment environment identifier
# Options: development, staging, production
# This affects behavior like error reporting and performance optimizations
ENVIRONMENT=development

# =============================================================================
# Additional Notes
# =============================================================================
# See specs/001-deployment-infrastructure/spec.md FR-017 for details on:
# - Secrets management best practices
# - Migration to cloud secret managers (Phase 3)
# - Security considerations for different environments
#
# For local development with Docker Compose:
# 1. Copy this file: cp .env.development.example .env.development
# 2. Update DATABASE_URL password to match docker-compose.yml togather-db service
# 3. Generate secure values for JWT_SECRET and ADMIN_API_KEY
# 4. Ensure .env.development is in .gitignore (already configured)
